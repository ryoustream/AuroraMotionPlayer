#pragma once
// SceneClassifier.h — forward-compatible header
// The SceneClassifier class is defined internally in SceneClassifier.cpp
// External users should use SceneDetector::analyze() which integrates classification
#include "SceneDetector.h"
